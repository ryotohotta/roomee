$(function(){
    $(".button-collapse").sideNav();
    $('.collapsible').collapsible();
    /**
     * jTinder initialization
     */
    $(".a-info").click(function(){
      var href = $(this).attr('href');
      location.href = href;
    });
    $(".a-info").bind('touchstart',function() {
      event.preventDefault();
      $(this).trigger('click');
    });
    $(".a-avatar").click(function(){
      var href = $(this).attr('href');
      location.href = href;
    });
    $(".a-avatar").bind('touchstart',function() {
      event.preventDefault();
      $(this).trigger('click');
    });
    $(".btn-top").click(function(){
      var href = $(this).attr('href');
      location.href = href;
    });
    $(".btn-top").bind('touchstart',function() {
      event.preventDefault();
      $(this).trigger('click');
    });

    $(".swipe-cards").jTinder({
      // dislike callback
        onDislike: function (item) {
          // set the status text
        },
      // like callback
        onLike: function (item) {
          // ajaxでlikeを送信, item-liのid取得
          var id_num = item.attr("id");
          var id_name = "#like-button-" + id_num;
          $(id_name).trigger('click');

          // $.ajax({
          //   url: "articles/29/likes",
          //   type: 'POST',
          //   dataType: 'html',
          //   data: {like: {id: 1, user_id: 3, article_id: 29 }},
          // })
          // .done(function() {
          //   console.log("success");
          // })
          // .fail(function() {
          //   console.log("error");
          // });
        },
      animationRevertSpeed: 700,
      animationSpeed: 900,
      threshold: 1,
      likeSelector: '.like',
      dislikeSelector: '.dislike'
    });

    /**
     * Set button action to trigger jTinder like & dislike.
     */
    $('.actions .like, .actions .dislike').click(function(e){
      e.preventDefault();
      $(".swipe-cards").jTinder($(this).attr('class'));
    });

    $(document).ready(function(){
      $('.materialboxed').materialbox();
    });
    $(document).ready(function() {
      $('select').material_select();
    });

    $(document).ready(function(){
      $('ul.tabs').tabs();
    });
    $(document).ready(function(){
      $('ul.tabs').tabs('select_tab', 'tab_id');
    });
    $(document).ready(function(){
      $('.slider').slider({full_width: true});
    });
});