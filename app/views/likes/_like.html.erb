<% if user_signed_in? %>
  <% if article.like_user(current_user.id) %>
  <% else %>
    <%= button_to "", article_likes_path( :article_id => article.id ), :method => :post, id: "like-button-#{article.id}", remote: true %>
  <% end %>
<% end %>